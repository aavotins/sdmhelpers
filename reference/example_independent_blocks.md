# Example independent spatial blocks

Fold or block identifiers used to demonstrate independent-data
selection.

## Usage

``` r
example_independent_blocks
```

## Format

A `sf` object containing independent spatial-blocks as `POLYGON` and
their identifiers.

## Source

A reduced subset prepared for package examples from actual species
distribution modelling.
