% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_joint_folds.R
\name{select_joint_folds}
\alias{select_joint_folds}
\title{Select joint presence and background folds for independent testing}
\usage{
select_joint_folds(
  pres_folds,
  bg_folds,
  target_prop = 0.25,
  pres_bounds = c(0.2, 0.3),
  bg_bounds = c(0.2, 0.3),
  max_tries = 3000,
  bg_match_weight = 1,
  include_na = FALSE,
  seed = NULL,
  print_report = TRUE
)
}
\arguments{
\item{pres_folds}{A vector containing fold identifiers for presence
records. Fold identifiers may be numeric, character, factor, or another
atomic type coercible to character.}

\item{bg_folds}{A vector containing fold identifiers for background
records. Fold identifiers may be numeric, character, factor, or another
atomic type coercible to character.}

\item{target_prop}{A single numeric value strictly between \code{0} and \code{1}.
The desired proportion of in-scope presence records assigned to the
independent testing dataset. The default is \code{0.25}.}

\item{pres_bounds}{A numeric vector of length two giving the minimum and
maximum permitted proportions of presence records in the selected folds.
Values must lie between \code{0} and \code{1}, and the first value must not exceed
the second. The default is \code{c(0.20, 0.30)}.}

\item{bg_bounds}{A numeric vector of length two giving the minimum and
maximum permitted proportions of background records in the selected
folds. Values must lie between \code{0} and \code{1}, and the first value must not
exceed the second. The default is \code{c(0.20, 0.30)}.}

\item{max_tries}{A positive whole number giving the maximum number of
randomized fold orderings evaluated by the heuristic search. Larger
values may improve the selected solution when fold sizes are irregular,
at the cost of additional computation. The default is \code{3000}.}

\item{bg_match_weight}{A non-negative numeric value controlling the
importance of matching the selected background proportion to the
selected presence proportion. A value of \code{0} disables proportional
matching, although \code{bg_bounds} are still considered. Larger values place
progressively greater emphasis on making the two selected proportions
similar. The default is \code{1}.}

\item{include_na}{Logical. If \code{FALSE}, records with missing fold IDs are
excluded from fold-size calculations and cannot be selected. Their
corresponding values in the returned row-selection vectors are
\code{FALSE}. If \code{TRUE}, missing fold IDs are treated as one additional fold
represented internally and in the returned fold IDs by \code{"<NA>"}.
The default is \code{FALSE}.}

\item{seed}{\code{NULL} or a single integer-like value passed to
\code{\link[base:set.seed]{base::set.seed()}}. Supplying a seed makes the randomized search
reproducible. When \code{NULL}, the current random-number generator state is
used.}

\item{print_report}{Logical. If \code{TRUE}, print a summary of the selected
folds, sample sizes, proportions, iteration number, and final score.
The default is \code{TRUE}.}
}
\value{
Invisibly returns a named list with the following elements:
\itemize{
\item \code{selected_fold_ids}: A character vector containing the selected fold IDs.
\item \code{pres_selected_idx}: A logical vector of length \code{length(pres_folds)}. Values are \code{TRUE} for
presence records belonging to selected folds.
\item \code{bg_selected_idx}: A logical vector of length \code{length(bg_folds)}. Values are \code{TRUE} for
background records belonging to selected folds.
\item \code{summary}: A list containing:
\itemize{
\item \code{n_pres_total}: Total number of elements in \code{pres_folds}, including missing values.
\item \code{n_bg_total}: Total number of elements in \code{bg_folds}, including missing values.
\item \code{n_pres_in_scope}: Number of presence records considered during selection after
applying the requested missing-value handling.
\item \code{n_bg_in_scope}: Number of background records considered during selection after
applying the requested missing-value handling.
\item \code{n_pres_selected}: Number of selected in-scope presence records.
\item \code{n_bg_selected}: Number of selected in-scope background records.
\item \code{prop_pres_selected}: Proportion of in-scope presence records selected.
\item \code{prop_bg_selected}: Proportion of in-scope background records selected.
\item \code{target_prop}: Requested presence selection proportion.
\item \code{bounds_pres}: Requested presence proportion bounds.
\item \code{bounds_bg}: Requested background proportion bounds.
\item \code{target_pres_abs}: Rounded target number of presence records.
\item \code{target_bg_abs_range}: Integer lower and upper bounds for the selected background count.
\item \code{iterations}: Iteration at which the best solution was first encountered.
\item \code{score}: Score of the selected solution. Smaller values indicate
better agreement with the requested target, bounds, and
proportional matching criterion.
}
}
}
\description{
Selects a common set of complete fold IDs for an independent testing
dataset. The selected folds are chosen so that the proportion of selected
presence records is close to a requested target, while the proportions of
both presence and background records remain within user-defined bounds.
}
\details{
The same fold IDs are selected for both datasets. Consequently, all
presence and background records belonging to a selected fold are assigned
to the independent testing dataset.

This function is intended for spatial or otherwise grouped model
evaluation in which complete folds must be withheld from both presence and
background datasets. It does not select individual records independently.

Fold sizes are first counted separately for the presence and background
data. The union of the presence and background fold IDs defines the
candidate folds. A fold that occurs in only one dataset therefore has size
zero in the other dataset.

The function performs up to \code{max_tries} randomized greedy searches. For
each randomized ordering, candidate folds are added when doing so improves
the objective score or when additional presence records are needed to reach
the lower presence bound.

The objective score combines:

\enumerate{
\item deviation of the selected presence count from the requested
presence target;
\item penalties for selected presence counts outside \code{pres_bounds};
\item penalties for selected background counts outside \code{bg_bounds}; and
\item the absolute difference between selected presence and background
proportions, weighted by \code{bg_match_weight}.
}

Because complete folds are indivisible, it may be impossible to satisfy
all requested bounds or to attain \code{target_prop} exactly. The returned
result is the best solution encountered by the heuristic search; it is not
guaranteed to be the global optimum.

Search quality may be improved by increasing \code{max_tries}, especially when:

\itemize{
\item the number of folds is large;
\item fold sizes vary substantially;
\item presence and background fold-size distributions differ; or
\item the requested bounds are narrow.
}
}
\note{
The function optimizes the number of records assigned to the independent
dataset, not the number of selected folds. A small number of large folds
can therefore be preferred over a larger number of small folds.

The returned logical vectors can be used directly to separate testing and
training records:

\code{pres_test  <- pres_data[result$pres_selected_idx, ]}

\code{pres_train <- pres_data[!result$pres_selected_idx, ]}

\code{bg_test    <- bg_data[result$bg_selected_idx, ]}

\code{bg_train   <- bg_data[!result$bg_selected_idx, ]}
}
\section{Interpretation of missing fold IDs}{


With \code{include_na = FALSE}, missing fold IDs are outside the selection
problem. They are included in \code{n_pres_total} or \code{n_bg_total}, but excluded
from \code{n_pres_in_scope} or \code{n_bg_in_scope}.

With \code{include_na = TRUE}, all missing fold IDs are treated as belonging to
one common fold named \code{"<NA>"}. Therefore, selecting that fold selects every
record with a missing fold ID in the corresponding dataset.

To avoid ambiguity when \code{include_na = TRUE}, the input fold identifiers
should not contain an actual, non-missing fold ID equal to \code{"<NA>"}.
}

\section{Random-number generation}{


If \code{seed} is supplied, the function calls \code{\link[base:set.seed]{base::set.seed()}} and therefore
changes R's global random-number generator state. The same inputs, seed,
and R version should produce the same result.
}

\examples{
## Create fold assignments with unequal fold sizes.
pres_folds <- rep(
  paste0("fold_", 1:8),
  times = c(12, 15, 9, 18, 14, 11, 16, 10)
)

bg_folds <- rep(
  paste0("fold_", 1:8),
  times = c(80, 95, 70, 120, 90, 75, 110, 85)
)

result <- select_joint_folds(
  pres_folds = pres_folds,
  bg_folds = bg_folds,
  target_prop = 0.25,
  pres_bounds = c(0.20, 0.30),
  bg_bounds = c(0.20, 0.30),
  max_tries = 500,
  seed = 123,
  print_report = FALSE
)

result$selected_fold_ids
result$summary

## Extract selected records.
pres_test_folds <- pres_folds[result$pres_selected_idx]
bg_test_folds <- bg_folds[result$bg_selected_idx]

unique(pres_test_folds)
unique(bg_test_folds)

## Verify that both datasets use the same selected fold IDs.
setequal(
  unique(pres_test_folds),
  unique(bg_test_folds)
)

## Presence and background tables do not need to contain exactly the
## same folds. A missing fold in one dataset receives a size of zero.
pres_folds2 <- rep(letters[1:6], c(7, 13, 10, 15, 9, 11))
bg_folds2 <- rep(letters[c(1:5, 7)], c(60, 90, 75, 100, 70, 50))

result2 <- select_joint_folds(
  pres_folds = pres_folds2,
  bg_folds = bg_folds2,
  target_prop = 0.25,
  pres_bounds = c(0.15, 0.35),
  bg_bounds = c(0.15, 0.35),
  max_tries = 500,
  seed = 42,
  print_report = FALSE
)

result2$selected_fold_ids

## Missing fold IDs can be excluded.
pres_with_na <- c(pres_folds, NA, NA)
bg_with_na <- c(bg_folds, NA)

result3 <- select_joint_folds(
  pres_folds = pres_with_na,
  bg_folds = bg_with_na,
  include_na = FALSE,
  max_tries = 500,
  seed = 7,
  print_report = FALSE
)

result3$pres_selected_idx[is.na(pres_with_na)]

## Alternatively, all missing IDs can be treated as one selectable fold.
result4 <- select_joint_folds(
  pres_folds = pres_with_na,
  bg_folds = bg_with_na,
  include_na = TRUE,
  max_tries = 500,
  seed = 7,
  print_report = FALSE
)

result4$selected_fold_ids

}
\seealso{
\code{\link[base:table]{base::table()}}, \code{\link[base:set.seed]{base::set.seed()}}, \code{\link[base:sample.int]{base::sample.int()}}
}
