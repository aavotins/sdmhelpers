% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phenology_overlap_weights.R
\name{phenology_overlap_weights}
\alias{phenology_overlap_weights}
\title{Calculate phenological overlap weights among species}
\usage{
phenology_overlap_weights(
  target,
  group,
  target_code,
  target_id = "CODE",
  group_id = "CODE",
  target_doy = "DoY",
  group_doy = "DoY",
  type = "2",
  nbins = 1024,
  boundaries = NULL,
  min_n = 2L,
  na_rm = TRUE,
  ...
)
}
\arguments{
\item{target}{A data frame containing observations of one or more potential
target species.}

\item{group}{A data frame containing observations of the comparison species.}

\item{target_code}{Species code identifying the target species in \code{target}.}

\item{target_id}{Character string giving the species-code column in
\code{target}. Defaults to \code{"CODE"}.}

\item{group_id}{Character string giving the species-code column in
\code{group}. Defaults to \code{"CODE"}.}

\item{target_doy}{Character string giving the day-of-year column in
\code{target}. Defaults to \code{"DoY"}.}

\item{group_doy}{Character string giving the day-of-year column in
\code{group}. Defaults to \code{"DoY"}.}

\item{type}{Character string specifying the overlap measure passed to
\code{\link[overlapping:overlap]{overlapping::overlap()}}. Either \code{"1"} or \code{"2"}. Defaults to \code{"2"}.}

\item{nbins}{Integer giving the number of equally spaced points used for
comparing the estimated densities. Passed to \code{\link[overlapping:overlap]{overlapping::overlap()}}.
Defaults to \code{1024}.}

\item{boundaries}{Optional numeric vector of length two defining the lower
and upper boundaries over which overlap is evaluated. Defaults to \code{NULL}.}

\item{min_n}{Minimum number of finite day-of-year observations required for
the target species and each comparison species. Defaults to \code{2}.}

\item{na_rm}{Logical. If \code{TRUE}, non-finite day-of-year values are removed
before estimating densities. Defaults to \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link[overlapping:overlap]{overlapping::overlap()}} and
subsequently to \code{\link[stats:density]{stats::density()}}, for example \code{bw}, \code{adjust}, or
\code{kernel}.}
}
\value{
A data frame containing the comparison-species code, the estimated
overlap with the target species, and the normalized overlap weight.
}
\description{
Calculates the overlap between the day-of-year distribution of a target
species and the day-of-year distributions of multiple comparison species.
Kernel-density overlap is calculated separately for each comparison species
using \code{\link[overlapping:overlap]{overlapping::overlap()}}.
}
\details{
The resulting overlap estimates are additionally normalized across all
comparison species so that their weights sum to 1.

The target species is selected from \code{target} using \code{target_code} and the
column specified by \code{target_id}.

Each species in \code{group} is then compared independently with the target
species. The normalized weight for species \eqn{i} is

\deqn{w_i = O_i / \sum_j O_j}

where \eqn{O_i} is the estimated overlap between the target species and
comparison species \eqn{i}.

Missing overlap estimates are excluded from the denominator. If all valid
overlap estimates are zero, weights are returned as \code{NA}.

Note that \code{\link[overlapping:overlap]{overlapping::overlap()}} uses ordinary linear kernel-density
estimation. Day of year is circular, so observations near the beginning
and end of the year are not treated as adjacent.
}
\examples{
set.seed(123)

target <- data.frame(
  CODE = rep(c("TARGET1", "TARGET2"), each = 100),
  DoY = c(
    round(rnorm(100, 140, 20)),
    round(rnorm(100, 220, 20))
  )
)

group <- data.frame(
  CODE = rep(c("SP1", "SP2", "SP3"), each = 100),
  DoY = c(
    round(rnorm(100, 145, 20)),
    round(rnorm(100, 190, 20)),
    round(rnorm(100, 250, 20))
  )
)

phenology_overlap_weights(
  target = target,
  group = group,
  target_code = "TARGET1"
)

}
\references{
Pastore, M. (2018). Overlapping: an R package for estimating overlapping in
empirical distributions. Journal of Open Source Software, 3(32), 1023.
\doi{10.21105/joss.01023}

Pastore, M., & Calcagni, A. (2019). Measuring distribution similarities
between samples: A distribution-free overlapping index.
Frontiers in Psychology, 10, 1089.
\doi{10.3389/fpsyg.2019.01089}
}
\seealso{
\code{\link[overlapping:overlap]{overlapping::overlap()}}, \code{\link[stats:density]{stats::density()}}
}
