% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_fold_by_quota.R
\name{select_folds_by_quota}
\alias{select_folds_by_quota}
\title{Select Complete Fold Groups to Approximate a Target Sample Proportion}
\usage{
select_folds_by_quota(
  folds,
  target_prop = 0.25,
  bounds = c(0.2, 0.3),
  max_tries = 2000L,
  include_na = FALSE,
  seed = NULL,
  print_report = TRUE
)
}
\arguments{
\item{folds}{A vector of fold identifiers, with one value per observation.
Fold identifiers may be numeric, character, factor, logical, or another
atomic vector type that can be processed by \code{\link[base:table]{base::table()}}.}

\item{target_prop}{A single numeric value strictly between \code{0} and \code{1}
giving the desired proportion of rows assigned to the selected folds.
It must fall within the interval specified by \code{bounds}. The default is
\code{0.25}.}

\item{bounds}{A numeric vector of length two giving the minimum and maximum
acceptable proportions of rows assigned to the selected folds. Values
must satisfy
\verb{0 <= bounds[1] <= target_prop <= bounds[2] <= 1}.
The default is \code{c(0.20, 0.30)}.}

\item{max_tries}{A positive integer giving the maximum number of randomized
fold orderings to evaluate. The search may stop earlier if it finds a
solution containing exactly the target number of rows and lying within
\code{bounds}. The default is \code{2000}.}

\item{include_na}{Logical. If \code{FALSE}, observations with missing fold
identifiers are excluded from the fold-frequency table and are never
selected. If \code{TRUE}, all missing fold identifiers are treated as a single
fold group represented by \code{"<NA>"} in \code{selected_fold_ids}.}

\item{seed}{An optional single integer used to initialize the random-number
generator before the search. Use a fixed value to make the selected folds
reproducible. If \code{NULL}, the current random-number generator state is
used.}

\item{print_report}{Logical. If \code{TRUE}, print a summary of the selected
solution to the console. The returned object is invisible regardless of
this setting.}
}
\value{
Invisibly returns an object of class \code{"fold_quota_selection"}, which is a
list with the following components:
\itemize{
\item \code{selected_fold_ids}: A character vector containing the identifiers of the selected fold
groups. If missing values are included and selected, they are represented
by \code{"<NA>"}.
\itemize{
\item \code{selected_idx}: A logical vector with the same length as \code{folds}. Values are \code{TRUE} for
rows belonging to selected fold groups and \code{FALSE} otherwise.
\item \code{summary}: A list containing:
\itemize{
\item \code{n_rows_total}: Number of rows included in the selection problem. Missing fold
identifiers are omitted when \code{include_na = FALSE}.
\item \code{n_rows_selected}: Number of rows belonging to the selected folds.
\item \code{prop_selected}: Proportion of included rows belonging to the selected folds.
\item \code{bounds}: Requested lower and upper proportional bounds.
\item \code{row_bounds}: Integer lower and upper bounds used during selection.
\item \code{target_prop}: Requested target proportion.
\item \code{target_n}: Target number of selected rows after rounding.
\item \code{within_bounds}: Logical value indicating whether the returned solution lies within
the requested integer bounds.
\item \code{exact_target}: Logical value indicating whether the selected row count equals
\code{target_n}.
\item \code{n_groups}: Total number of fold groups considered.
\item \code{n_groups_selected}: Number of fold groups selected.
\item \code{iterations_attempted}: Number of randomized attempts performed.
\item \code{best_iteration}: Attempt on which the best returned solution was first found.
}
}
}
}
\description{
This function is intended for creating an independent testing subset when
observations have already been assigned to spatial, temporal, environmental,
or other grouped folds. It searches for a subset of complete fold groups
whose combined number of observations approximates \code{target_prop}.

Candidate subsets are generated using repeated random orderings of the fold
groups. For each ordering, groups are greedily added while their cumulative
size does not exceed the upper bound. If the resulting subset is smaller
than the lower bound, one additional group is selected to improve the
solution.

Because folds are indivisible, an exact match to the target proportion may
not be possible. The function therefore returns the best solution found
during at most \code{max_tries} randomized attempts. In some cases, particularly
when one or more folds are very large, no combination may satisfy
\code{bounds}. The returned summary indicates whether the selected solution lies
within the requested bounds.

Missing fold identifiers are excluded by default. When \code{include_na = TRUE},
all missing identifiers are treated as one additional fold group.
}
\details{
Selects complete fold groups so that the number of rows belonging to the
selected folds is as close as possible to a requested proportion of all
rows. Fold groups are never split between the selected and unselected sets.

The row-count constraints are calculated as:

\preformatted{
lower   = ceiling(bounds[1] * N)
upper   = floor(bounds[2] * N)
target  = round(target_prop * N)
}

where \code{N} is the number of observations included in the selection problem.

The procedure is a randomized heuristic rather than an exhaustive
combinatorial search. Consequently, it does not guarantee the globally
optimal combination of folds. Increasing \code{max_tries} can improve the
probability of finding a good combination when many fold groups are
available.

The same fold identifiers and seed produce the same result under the same
R version and random-number generator settings. Calling this function with
a non-\code{NULL} \code{seed} changes the global random-number generator state.

Fold identifiers in \code{selected_fold_ids} are returned as character values
because \code{\link[base:table]{base::table()}} uses group labels as names. Use \code{selected_idx} when
subsetting the original data, because it preserves the direct relationship
with the input rows and avoids type-conversion issues.
}
\section{Missing fold identifiers}{

When \code{include_na = FALSE}, rows with missing fold identifiers:
\itemize{
\item are not included in \code{n_rows_total};
\item do not affect the target or bounds;
\item receive \code{FALSE} in \code{selected_idx}.
}

When \code{include_na = TRUE}, all missing fold identifiers form one complete
group. Therefore, either all or none of the missing-fold rows are selected.
}

\section{Interpretation}{

This function selects complete fold groups based only on their sizes. It does
not assess whether the selected observations are environmentally,
geographically, temporally, or statistically independent. Independence must
arise from the procedure used to define \code{folds}.
}

\examples{
## Example with equally sized folds
folds <- rep(letters[1:10], each = 10)

selection <- select_folds_by_quota(
  folds,
  target_prop = 0.25,
  bounds = c(0.20, 0.30),
  seed = 123,
  print_report = FALSE
)

selection$selected_fold_ids
selection$summary
table(selection$selected_idx)

## Use selected_idx to divide a data frame
dat <- data.frame(
  occurrence_id = seq_along(folds),
  fold = folds
)

independent_test <- dat[selection$selected_idx, , drop = FALSE]
model_training <- dat[!selection$selected_idx, , drop = FALSE]

## Verify that complete folds were selected
unique(independent_test$fold)
intersect(
  unique(independent_test$fold),
  unique(model_training$fold)
)

## Unequal fold sizes
unequal_folds <- rep(
  paste0("fold_", 1:8),
  times = c(5, 8, 12, 15, 18, 20, 7, 15)
)

unequal_selection <- select_folds_by_quota(
  unequal_folds,
  target_prop = 0.25,
  bounds = c(0.20, 0.30),
  max_tries = 5000,
  seed = 42,
  print_report = FALSE
)

unequal_selection$summary$prop_selected
unequal_selection$summary$within_bounds

## Missing fold identifiers excluded
folds_with_na <- c("A", "A", "B", "B", "C", NA, NA)

without_na <- select_folds_by_quota(
  folds_with_na,
  target_prop = 0.4,
  bounds = c(0.3, 0.5),
  include_na = FALSE,
  seed = 1,
  print_report = FALSE
)

without_na$selected_idx

## Treat all missing identifiers as one fold
with_na <- select_folds_by_quota(
  folds_with_na,
  target_prop = 0.4,
  bounds = c(0.3, 0.5),
  include_na = TRUE,
  seed = 1,
  print_report = FALSE
)

with_na$selected_fold_ids
with_na$selected_idx

}
\seealso{
\code{\link[base:table]{base::table()}}, \code{\link[base:sample.int]{base::sample.int()}}, \code{\link[base:set.seed]{base::set.seed()}}
}
